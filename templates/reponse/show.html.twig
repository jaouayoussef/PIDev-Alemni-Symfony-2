<!DOCTYPE html>

<title>Reponse</title>

{% block body %}
    <h1>Reponse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reponse.id }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ reponse.answer }}</td>
            </tr>
            <tr>
                <th>File</th>
                <td>{{ reponse.file }}</td>
            </tr>
            <tr>
                <th>ReplyDate</th>
                <td>{{ reponse.replyDate ? reponse.replyDate|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reponse_index') }}">back to list</a>

    <a href="{{ path('reponse_edit', {'id': reponse.id}) }}">edit</a>

    {{ include('reponse/_delete_form.html.twig') }}
{% endblock %}
