<form method="post" action="{{ path('useranswer_delete', {'id': useranswer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ useranswer.id) }}">
    <button class="btn">Delete</button>
</form>
