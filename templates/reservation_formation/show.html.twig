{% extends 'base.html.twig' %}

{% block title %}ReservationFormation{% endblock %}

{% block body %}
    <h1>ReservationFormation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_formation.id }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ reservation_formation.prix }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ reservation_formation.date ? reservation_formation.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('reservation_formation_index') }}">back to list</a>

    <a href="{{ path('reservation_formation_edit', {'id': reservation_formation.id}) }}">edit</a>

    {{ include('reservation_formation/_delete_form.html.twig') }}
{% endblock %}
