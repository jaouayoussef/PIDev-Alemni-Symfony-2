<form method="post" action="{{ path('reservation_formation_delete', {'id': reservation_formation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reservation_formation.id) }}">
    <button class="btn">Delete</button>
</form>
