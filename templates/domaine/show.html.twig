{% extends 'base.html.twig' %}

{% block title %}Domaine{% endblock %}

{% block body %}
    <h1>Domaine</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ domaine.id }}</td>
            </tr>
            <tr>
                <th>NomDomaine</th>
                <td>{{ domaine.nomDomaine }}</td>
            </tr>
            <tr>
                <th>DescriptionDomaine</th>
                <td>{{ domaine.descriptionDomaine }}</td>
            </tr>
            <tr>
                <th>ImageDomaine</th>
                <td>{{ domaine.imageDomaine }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('domaine_index') }}">back to list</a>

    <a href="{{ path('domaine_edit', {'id': domaine.id}) }}">edit</a>

    {{ include('domaine/_delete_form.html.twig') }}
{% endblock %}
